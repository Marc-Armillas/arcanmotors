<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-2xl font-bold mb-6">Listado de Solicitudes de Contacto</h1>

  <div class="flex flex-col">
    <div class="overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <div class="shadow overflow-hidden border-b border-gray-200 sm:rounded-lg">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Nombre</th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Teléfono</th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Asunto</th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Mensaje</th>
              <th scope="col" class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Coche (ID)</th>
              <th scope="col" class="relative px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
            <% @contact_requests.each do |request| %>
              <tr>
                <td class="px-6 py-4 whitespace-nowrap text-center text-sm font-medium text-gray-900"><%= request.name %></td>
                <td class="px-6 py-4 whitespace-nowrap text-center text-sm text-gray-500"><%= request.email %></td>
                <td class="px-6 py-4 whitespace-nowrap text-center text-sm text-gray-500"><%= request.phone %></td>
                <td class="px-6 py-4 whitespace-nowrap text-center text-sm text-gray-500"><%= request.subject %></td>
                <td class="px-6 py-4 whitespace-nowrap text-center text-sm text-gray-500"><%= request.message %></td>
                <td class="px-6 py-4 whitespace-nowrap text-center text-sm text-gray-500"><%= request.car_id %></td>
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                  <div class="grid grid-cols-5 items-center">
                    <div></div>
                    <div class="flex justify-center"> <!-- Posición 2: Primer icono -->
                      <%= link_to '👁️', admin_contact_request_path(request) %>
                    </div>
                    <div></div>
                    <div class="flex justify-center"> <!-- Posición 4: Segundo icono -->
                      <%= link_to '🗑️', admin_contact_request_path(request), method: :delete, data: { confirm: '¿Estás seguro de que deseas eliminar esta solicitud de contacto?' } %>
                    </div>
                    <div></div>
                  </div>
                </td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>