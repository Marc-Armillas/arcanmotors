<!DOCTYPE html>
<html>
  <head>
    <title><%= "ArCanmotors" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="<%= asset_path('arcan_logo.png') %>" type="image/png">

    <%#= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
    <%= stylesheet_pack_tag "application", "data-turbolinks-track": "reload" %>
    <%= javascript_pack_tag "application", "data-turbolinks-track": "reload" %>

    <script defer src="https://cloud.umami.is/script.js" data-website-id="<% Rails.application.credentials.dig(:unami, :arcanmotors_id).presence %>"></script>
  </head>

  <body class="bg-gray-100">
    <%= render "shared/header" %>
    <% if flash.any? %>
      <div class="container mx-auto mt-4">
        <% flash.each do |type, message| %>
          <div class="flash-message <%= flash_class(type) %>"><%= message %></div>
        <% end %>
      </div>
    <% end %>

    <% if controller_name == 'static_pages' && action_name == 'home' %>
      <%=  render "shared/banner" %>
    <% end %>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <%= yield %>
    </div>

    <%= render "shared/whatsapp_button" %>

    <%= render "shared/footer" %>
  </body>
</html>
